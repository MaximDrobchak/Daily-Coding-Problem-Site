import React from 'react';
import { Layout, ContainerPage, Typed } from '../';
export default ({ color }) => (
  <Layout>
    <ContainerPage>
      <Typed />
      <h1>sdfdfdf</h1>
    </ContainerPage>
  </Layout>
);
